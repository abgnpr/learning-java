# Completion Estimate — `spring-boot-basics.md`

*Set 2026-07-21.*

**Baseline assumed:** only IMPS-level Spring — `@RestController`,
`@SpringBootApplication`, basic `@Autowired`, and **raw JDBC (no
JPA)**. So §7 (JPA) and §8 (transactions/proxies) are effectively new.

Estimate is for the **"drilled to parity"** bar — explain each 💭
aloud + build/run the 🛠 in a scratch app — **not** read-through.
Reading ≠ loading.

| Scope | Sections | Time (parity) |
|---|---|---|
| **Pareto core path** | §1→§4→§3→§5→§7→§8 + §0 skim + §11 | **~13–15 h** |
| **Full doc** | + §2, §6, §9, §10 breadth | **~18–22 h** |
| Read-through only | no builds — racked, not loaded | ~7–8 h (core) |

**Cadence:** 1 h/day → core in ~2.5–3 weeks · ~5 h/wk → full in ~4–5 weeks.

## Time sinks = highest ROI (IMPS never taught them)

- **§7 data/JPA** — you used raw JDBC; Spring Data JPA is new ground.
- **§8 transactions & proxies** — deep, and the most-probed in interviews.

Together ≈ 6 of the ~14 core hours. **Weekend-only crunch:**
§7 + §8 + §4 (auto-config) = the 20%-of-the-20%.

---

*Kit indexed in `../CLAUDE.md` drill-kits table (status 🟡 racked).*
